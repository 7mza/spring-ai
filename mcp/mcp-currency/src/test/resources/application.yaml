custom:
  currency:
    api: https://api.frankfurter.dev/v2/rate
logging:
  include-application-name: false
server:
  port: 0
  ssl:
    enabled: false
spring:
  config:
    activate:
      on-profile: default
  main:
    banner-mode: off
  output:
    ansi:
      enabled: always
